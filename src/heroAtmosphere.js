export function installHeroAtmosphere(){
 const layer=document.getElementById('sakinah-atmosphere-overlay');
 if(layer)layer.remove();
 const styles=document.getElementById('mm-weather-keyframes');
 if(styles)styles.remove();
}
