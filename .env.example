# Quran Foundation Content API — backend only
# Request credentials from Quran.Foundation. Never expose the secret to Vite/browser code.
QF_ENV=prelive
QF_CLIENT_ID=
QF_CLIENT_SECRET=

# Local backend port for the authenticated proxy
SAKINAH_API_PORT=8787
